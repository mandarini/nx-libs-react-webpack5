import type { Meta } from '@storybook/react';
import { ReactNb } from './react-nb';

const Story: Meta<typeof ReactNb> = {
  component: ReactNb,
  title: 'ReactNb',
};
export default Story;

export const Primary = {
  args: {},
};
